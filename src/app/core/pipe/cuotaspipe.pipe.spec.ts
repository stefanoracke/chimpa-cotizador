import { CuotaspipePipe } from './cuotaspipe.pipe';

describe('CuotaspipePipe', () => {
  it('create an instance', () => {
    const pipe = new CuotaspipePipe();
    expect(pipe).toBeTruthy();
  });
});
